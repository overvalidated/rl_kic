

Среда:
    10 возможных действий (выбор часов от 4 до 12 + 0 часов)
    Агент поочередно выбирает количество часов для работы сотрудника внутри дня t, затем переходит к дню t+1
    Terminate если выполняется stop-условие:
        1) после дневной смены следует ночная, после ночной - дневня
        2) работает шесть дней подряд
        3) за 7 дней работает > 42 часов

Красота шаблона учитывается следующим образом (needs testing):
(SHIT)
    1) строим дерево на шаблоне (X = {1, 2, 3, 4...} натуральные числа, Y = {1,1,0,0,1,1,0,0} - вывод сотрудника в день)
    2) берем среднюю длину пути от корня до листьев.
(GOOD)
    Берется средняя автокорреляция (pacf).

Агент:
Resnet, input_shape: 
        (кол-во сотрудников, 
         кол-во смен, 
         10 возможных часов - 
         - 1 мультиколлинеарность + 
         + 1 распределение часов в дне +
         + 1 распределение часов в месяц с недоработками/переработками)
а resnet ли?

Обучение: 
    